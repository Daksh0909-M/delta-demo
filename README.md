this is delta demo
hi
delta-demo
├── .git
└── README.md

1. .
Current directory (the folder you're standing in).
Right now:
. = delta-demo


2. ..
Parent directory (one level above).
So:.. = gitdemo
because:
gitdemo
└── delta-demo   ← you are here

3. .git/
.git/

This is a hidden folder.

This is the heart of Git.

It contains things like:

commit history
branches
configuration
references
objects

./  ../  .git/  README.md
Item	Meaning
.	        Current folder (delta-demo)
..	        Parent folder (gitdemo)
.git/	    Hidden Git folder
README.md	Normal file
